#include "main.h"
/**
 * print_last_digit - a function that prints the last digit of a number.
 * @n:  An int argument
 * Return: 0
 */
int print_last_digit(int n)
{
if (n > 0 || n == 0)
{
_putchar (n % 10 + '0');
return (n % 10);
}
else if
{
return (88);
}
else
{
n = n * -1;
_putchar (n % 10 + '0');
return (n % 10);
}
}
