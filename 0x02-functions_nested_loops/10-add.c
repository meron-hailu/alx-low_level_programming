#include "main.h"
/**
 * add - a function which adds two numbers
 * @a: is an int type first argument
 * @b: is the second argument of type int
 * Return: 0
 */
int add(int a, int b)
{
int result;
result = a + b;
return (result);
}
