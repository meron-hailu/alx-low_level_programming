#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 **/
int main (void)
{
char out[8] = {'_', 'p', 'u', 't', 'c', 'h', 'a', 'r'};
unsigned int i;
for (i = 0; i < sizeof(out); i++)
{
_putchar(out[i]);
}
_putchar('\n');  
return (0);
}  
